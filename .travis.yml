language: generic

dist: trusty
sudo: required

addons:
  apt:
    packages:
      - emacs

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y rakudo
  - sudo apt-get install -y leiningen

script:
  - make -k
